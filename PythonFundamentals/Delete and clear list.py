fruits = ["fig", "banan", "lemon"]
fruits.remove("banan")
#print(fruits)
#Pn, remove is used by specifying which item to remove, not index. 
#For index, we use pop. 
#fruits.pop(1)
#print(fruits)
#Pop() removes the last Item by default 
#Delete the complete list or an indexed item by using del 
#del fruits
#Clearing the list is done by using fruit.clear
fruits.clear()
print(fruits)
